<div class="jumbotron">
    <div class="container">
        {{> flash}}
        <h2>{{items.current_user}}'s Profile:</h2>
        {{#if_eq items.current_user username}}
            <h2>Create a new Project:
                <a href="/projects/create" class="btn btn-success">Create</a>
            </h2>
        {{/if_eq}}
        <h2>Current open Projects:</h2>
        <table class="table">
            <thead>
            <tr>
                <td>Project Name</td>
                <td>Project Description</td>
                <td>Date Created</td>
                {{#if_eq items.current_user username}}
                    <td>Edit project</td>
                    <td style="float:right">Delete project</td>
                {{/if_eq}}
            </tr>
            </thead>
            {{#each items.items as |result|}}
                <tbody>
                <tr>
                    <td><a href="/projects/{{result.obj._id}}">{{result.obj.name}}</a></td>
                    <td>{{result.obj.description}}</td>
                    <td>{{result.obj.date}}</td>
                    {{#if_eq ../items.current_user ../username}}
                        <td>
                            {{#if_eq result.update true}}
                                <form method="GET" action="/projects/{{result.obj._id}}/edit">
                                    <button type="submit" class="btn btn-default"> Edit Project</button>
                                </form>
                            {{/if_eq}}
                        </td>
                        <td>
                            {{#if_eq result.delete true}}
                                <form method="POST" action="/projects/{{result.obj._id}}?_method=DELETE"
                                      style="float:right">
                                    <button type="submit" class="btn btn-danger"> X</button>
                                </form>
                            {{else}}
                                <form method="POST" action="/projects/{{result.obj._id}}/leave?_method=DELETE"
                                      style="float:right">
                                    <button type="submit" class="btn btn-default"> Leave Project</button>
                                </form>
                            {{/if_eq}}
                        </td>
                    {{/if_eq}}
                </tr>
                </tbody>
            {{/each}}
        </table>
    </div>
</div>
<!--<div class="container">-->
<!--&lt;!&ndash; Example row of columns &ndash;&gt;-->
<!--<div class="row">-->
<!--<div class="col-md-4">-->

<!--</div>-->
<!--</div>-->
<!--<footer>-->
<!--<p>&copy; 2016 Company, Inc.</p>-->
<!--</footer>-->
<!--</div>-->
